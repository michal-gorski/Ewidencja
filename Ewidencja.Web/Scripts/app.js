angular.module('ewidencja', ['ewidencjaServices']);
/*
    . config(['$routeProvider', function ($routeProvider) {
      $routeProvider.
          when('/DoctorDashboard', { controller: PatientsListController }).
          otherwise({ redirectTo: '/DoctorDashboard' });
  }]);

  */